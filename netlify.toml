# ===========================================
#  Netlify Configuration
# ===========================================

[build]
  # Le script qui génère config.js avant le déploiement
  command = "bash build.sh"

  # Dossier à publier (racine du projet = site statique)
  publish = "."
